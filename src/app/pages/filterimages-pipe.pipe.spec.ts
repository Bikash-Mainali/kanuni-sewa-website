import { FilterimagesPipePipe } from './filterimages-pipe.pipe';

describe('FilterimagesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterimagesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
